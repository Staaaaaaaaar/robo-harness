# Tasks

Task implementations live under `tasks/<task>/`. They define validated task
semantics and termination conditions without depending on a simulator backend.

The PointNav implementation is introduced by PR 08.
